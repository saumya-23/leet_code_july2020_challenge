Given an input string, reverse the string word by word.

Example 1:

Input: "the sky is blue"
Output: "blue is sky the"
Example 2:

Input: "  hello world!  "
Output: "world! hello"
Explanation: Your reversed string should not contain leading or trailing spaces.
Example 3:

Input: "a good   example"
Output: "example good a"
Explanation: You need to reduce multiple spaces between two words to a single space in the reversed string.

Explanation

Simply trim the string to avoid blank spaces before and after the string.
Then store each word in an List by using split method.
Then reverse the string using reverse method of Collection.
Then return string using join method of list.